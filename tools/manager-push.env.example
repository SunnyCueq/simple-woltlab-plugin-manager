# manager-push.env – lokale Overrides für release-manager.sh (optional)
# Kopiere nach manager-push.env — wird von Git ignoriert.
#
# SWPM_RELEASE_BRANCH="main"
#
# Git-Identität (falls git config leer):
# GIT_AUTHOR_NAME="…"
# GIT_AUTHOR_EMAIL="…@…"
